1)  Διαφορά new με Mutable δηλαδή 

let mutable t in
t := Nil

είναι ισοδύναμο με

let t = new tree in
t := Nil

πως λειτουργεί το delete σε κάθε περίπτωση ( Παραδείγματα σε source code )

2) f σε ref

3) lineno

4) types και vars με ιδιο όνομα

5) f x y = x == y
f 1 1
f 'a' 'a'